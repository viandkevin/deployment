apiVersion: apps/v1
kind: Deployment
metadata:
  name: next-app
  namespace: space
spec:
  replicas: 1
  selector:
    matchLabels:
      app: next-app
      version: v1
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        app: next-app
        version: v1
    spec:
      containers:
        - name: next-app
          image: pro24/next-app:v5
          ports:
            - containerPort: 3000
          env:
            - name: SPACE_SERVICE_HOST
              value: "http://space-service:8080"
            - name: SPACE_SERVICE_ENDPOINT
              value: "/api/hello"
            - name: VERSION
              value: "v2"
            - name: BACKEND_VERSION
              value: "v6"
          resources:
            limits:
              memory: "500Mi"
            requests:
              memory: "400Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: next-app
  namespace: space
spec:
  type: ClusterIP
  selector:
    app: next-app
    version: v1
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP